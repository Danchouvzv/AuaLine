import CartContent from './CartContent';

export const metadata = {
  title: 'Your Cart | AuaLine',
  description: 'View and manage your AuaLine shopping cart',
};

export default function CartPage() {
  return <CartContent />;
} 